rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client/browser access. All reads and writes must route through the authenticated Express API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

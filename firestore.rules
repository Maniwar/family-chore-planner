rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Multi-tenant household document
    match /households/{householdId} {
      allow read, write: if true;

      match /{document=**} {
        allow read, write: if true;
      }
    }
  }
}
